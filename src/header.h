#include<stdio.h>
#include<stdlib.h>
#include <sys/stat.h>
int isPrime(int );
int thirdQuest(char * );
int getMaxDiff(int );
int getSum(int );
void secQuest(char *);
int inputFile();
int isInputValid(char []);
void showError();
void eeke(char *);
int isFilePresent(char *);
void checkFileSysytem();
int isInputPresent(char *);
